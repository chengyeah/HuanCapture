package org.webrtc.ext;import android.util.Log;import androidx.annotation.CallSuper;import org.webrtc.IceCandidate;import org.webrtc.SessionDescription;/** * <br> * * <br> */public class TransferAdapter {    private static final String TAG = "transfer";    private RemoteCallback mAnswerCallback;    //region 握手    @CallSuper    public void sendOffer(SessionDescription sdp) {        Log.d(TAG, "send offer:" + sdp.description);    }    @CallSuper    public void sendAnswer(SessionDescription sdp) {        Log.d(TAG, "send answer:" + sdp.description);    }    //endregion    @CallSuper    public void sendIceCandidate(IceCandidate iceCandidate) {        Log.d(TAG, "send ice");    }    public void setRemoteCallback(RemoteCallback callback) {        mAnswerCallback = callback;    }    public RemoteCallback getAnswerCallback() {        return mAnswerCallback;    }    public interface RemoteCallback {        void onReceiveOffer(SessionDescription sdp);        void onReceiveAnswer(SessionDescription sdp);        void onReceiveIceCandidate(IceCandidate ice);    }}